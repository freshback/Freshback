<div class='album'>
  <div class='container'>
    <div class='row'>
      <div class='col-md-6 text-center mb-sm-5'>
        <img style='max-width: 100%;' src='<%= @keshback.photo %>' alt=''/>
      </div>
      <div class='col-md-6 mb-sm-5'>
        <div class='card'>
          <div class='card-body'>
            <h1>
              <%= @keshback.name %>
            </h1>
            <p>
              <%= link_to "Link", "https://#{@keshback.link}" %>
            </p>
            <h5>
              <%= @keshback.count %>
              %
            </h5>
            <% if user_signed_in? %>

              <%= render 'comments/form'%>

            <% else %>
              <%= link_to "Reg", new_user_registration_path%><br>
              або
              <%= link_to "Log", new_user_session_path%>
            <% end %>
          </div>
        </div>
      </div>
    </div>
    <div class='mb-sm-5'>
      <%= @keshback.description %>
    </div>
  </div>
  <ul class="content-list content-list_comments">
    <% @comments.each do |comment| %>
      <%=render 'comments/comment', comment:comment %>
    <%end%>
  </ul>
</div>
