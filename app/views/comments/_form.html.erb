<%= form_for :comment, url: comments_path do |f| %>
<%= f.text_area :text, class: "form-control", :rows => 4 %>
<%= f.hidden_field :keshback_id, value: @keshback.id %>
<%= f.submit "Comment", class:"btn btn-primary" %>
<%end%>
